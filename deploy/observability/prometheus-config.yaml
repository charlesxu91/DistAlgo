apiVersion: v1
kind: ConfigMap
metadata:
  name: distalgo-prometheus-config
data:
  prometheus.yml: |
    scrape_configs:
      - job_name: distalgo
        metrics_path: /metrics
        static_configs:
          - targets:
              - distalgo:8000
